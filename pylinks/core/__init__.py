"""core — czyste helpery bez zależności od GUI/platform/sieci.

Faza 1: naming (safe_name), matching (name_similarity, needs_disambiguation),
rom_args (_rom_build_args), compression (stuby zstd/gzip).
"""
